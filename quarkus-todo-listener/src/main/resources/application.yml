mp:
  messaging:
    incoming:
      todocompletions:
        broadcast: true
        connector: smallrye-kafka
        topic: todocompletions
        auto:
          offset:
            reset: earliest
